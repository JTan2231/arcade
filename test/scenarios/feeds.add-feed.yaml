name: add feed loads sources, previews, validates, and creates

vars:
  email: add-feed-owner@example.test
  password: password123

before:
  - request:
      id: signup_owner
      method: POST
      path: /api/auth/signup
      json:
        display_name: Add Feed Owner
        email: "{{email}}"
        password: "{{password}}"
        remember_me: false
      expectStatus: 201
      capture:
        owner_id: $.id

  - request:
      id: login_owner
      client: browser
      method: POST
      path: /api/auth/login
      json:
        email: "{{email}}"
        password: "{{password}}"
        remember_me: false
      expectStatus: 200

  - request:
      id: create_group
      client: browser
      method: POST
      path: /api/groups
      json:
        name: Add Feed Dojo
      expectStatus: 201
      capture:
        group_id: $.id

  - request:
      id: create_source
      client: browser
      method: POST
      path: /api/groups/{{group_id}}/catalog-sources
      json:
        name: Add Feed Source
        template: https://example.test/{slug}
        fields:
          - key: difficulty
            label: Difficulty
            value_type: number
            is_array: false
            display_order: 0
        items:
          - title: Alpha Warmup
            data:
              slug: alpha-warmup
              difficulty: 800
          - title: Beta Warmup
            data:
              slug: beta-warmup
              difficulty: 1200
      expectStatus: 201
      capture:
        source_id: $.id

steps:
  - visit: /

  - holdRequest:
      id: sources
      method: GET
      path: /api/groups/*/catalog-sources
      times: 1

  - within: Feeds
    click:
      role: button
      name: Add feed

  - expectRequest:
      id: sources

  - within: Add feed
    click:
      role: button
      name: Next

  - within: Add feed
    expectAlert:
      text: Name is required

  - within: Add feed
    fill:
      label: Name
      value: Warmup Practice

  - within: Add feed
    click:
      role: button
      name: Next

  - within: Add feed
    click:
      role: button
      name: Next

  - within: Add feed
    expectDisabled:
      role: combobox
      name: Source

  - within: Add feed
    expectDisabled:
      role: button
      name: Next

  - releaseRequest: sources

  - within: Add feed
    expectEnabled:
      role: combobox
      name: Source

  - within: Add feed
    expectEnabled:
      role: button
      name: Next

  - within: Add feed
    fill:
      label: Item count
      value: 1

  - within: Filters
    click:
      role: button
      name: Add filter

  - within: Filters
    fill:
      label: Value
      value: 800

  - within: Add feed
    click:
      role: button
      name: Next

  - within: Add feed
    click:
      role: button
      name: Next

  - holdRequest:
      id: preview
      method: POST
      path: /api/groups/*/daily-feeds/preview
      times: 1

  - within: Add feed
    click:
      role: button
      name: Preview

  - expectRequest:
      id: preview

  - within: Add feed
    expectDisabled:
      role: button
      name: Add feed

  - releaseRequest: preview

  - within: Preview
    expectVisible:
      text: 1 selected, 1 eligible

  - within: Add feed
    click:
      role: button
      name: Add feed

  - expectStatus:
      text: Feed created

  - within: Feeds
    expectPressed:
      role: button
      name: Warmup Practice
      pressed: true

  - within: Selected feed output
    expectVisible:
      text: Warmup
